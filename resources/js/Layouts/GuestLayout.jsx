const GuestLayout = ({ children }) => {
   return (
      <div className="min-h-screen flex justify-center items-center p-7 bg-[#F5F5F5]">
         {children}
      </div>
   );
};

export default GuestLayout;
